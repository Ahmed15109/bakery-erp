<Window x:Class="Bakery.WPF.Views.RecoveryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FlowDirection="RightToLeft">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <materialDesign:ColorZone Mode="PrimaryMid" Padding="16">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="AlertCircleOutline" Width="32" Height="32" VerticalAlignment="Center"/>
                <TextBlock Text="مركز التعافي من الطوارئ وإصلاح البيانات" Margin="16,0,0,0" Style="{StaticResource MaterialDesignHeadline5TextBlock}" VerticalAlignment="Center"/>
            </StackPanel>
        </materialDesign:ColorZone>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Side: Backup History -->
            <GroupBox Header="قائمة النسخ الاحتياطية المتوفرة" Style="{StaticResource MaterialDesignCardGroupBox}" materialDesign:ColorZoneAssist.Mode="Accent">
                <Grid>
                    <DataGrid ItemsSource="{Binding Backups}" 
                              SelectedItem="{Binding SelectedBackup}"
                              AutoGenerateColumns="False" 
                              IsReadOnly="True" 
                              SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="التاريخ" Binding="{Binding CreatedAt, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="*"/>
                            <DataGridTextColumn Header="اسم الملف" Binding="{Binding FileName}" Width="*"/>
                            <DataGridTextColumn Header="الحجم" Binding="{Binding SizeBytes, Converter={StaticResource CommonConverters.FileSizeConverter}}" Width="Auto"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </GroupBox>

            <!-- Right Side: Actions -->
            <StackPanel Grid.Column="1" Margin="24,0,0,0">
                <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" Style="{StaticResource MaterialDesignBody1TextBlock}" Margin="0,0,0,24" Foreground="{DynamicResource MaterialDesignSecondaryText}"/>
                
                <Button Command="{Binding RestoreCommand}" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,0,12" materialDesign:ButtonAssist.CornerRadius="8" Height="50" Background="{DynamicResource PrimaryHueMidBrush}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Restore" Margin="0,0,8,0"/>
                        <TextBlock Text="استعادة النسخة المحددة"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding RepairDbCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,0,12" materialDesign:ButtonAssist.CornerRadius="8" Height="50">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Tools" Margin="0,0,8,0"/>
                        <TextBlock Text="إصلاح السجلات اليتيمة"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding ExportLogsCommand}" Style="{StaticResource MaterialDesignFlatButton}" Margin="0,0,0,12" materialDesign:ButtonAssist.CornerRadius="8" Height="50">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="FileExport" Margin="0,0,8,0"/>
                        <TextBlock Text="تصدير سجلات الأخطاء"/>
                    </StackPanel>
                </Button>

                <Separator Margin="0,12"/>

                <Button Command="{Binding ExitAppCommand}" Style="{StaticResource MaterialDesignFlatButton}" Foreground="{DynamicResource MaterialDesignSecondaryText}" Height="50">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ExitToApp" Margin="0,0,8,0"/>
                        <TextBlock Text="إغلاق النظام"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Footer / Busy Overlay -->
        <ProgressBar Grid.Row="2" IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="0,0,0,8"/>
    </Grid>
</Window>
