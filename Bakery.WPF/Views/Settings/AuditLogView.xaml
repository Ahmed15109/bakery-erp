<UserControl x:Class="Bakery.WPF.Views.AuditLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="#FBF9F7" FlowDirection="RightToLeft">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Grid Margin="0,0,0,18">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="سجل التدقيق" FontSize="26" FontWeight="Bold" Foreground="#2D2422"/>
                <TextBlock Text="تتبع المستخدم والوقت والعملية والنتيجة لكل إجراء حساس." FontSize="13" Foreground="#8B7E7A"/>
            </StackPanel>
            <Button Grid.Column="1" Command="{Binding ExportCommand}" Height="42" Padding="16,0"
                    Style="{StaticResource MaterialDesignOutlinedButton}" BorderBrush="#8B5E4C" Foreground="#8B5E4C">
                <StackPanel Orientation="Horizontal"><md:PackIcon Kind="FileExport" Margin="0,0,6,0"/><TextBlock Text="تصدير CSV"/></StackPanel>
            </Button>
        </Grid>

        <md:Card Grid.Row="1" UniformCornerRadius="12" Background="White" Padding="14" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="170"/><ColumnDefinition Width="170"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" md:HintAssist.Hint="بحث بالمستخدم أو العملية أو الكيان..."
                         md:TextFieldAssist.HasClearButton="True" Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,8,0"/>
                <DatePicker Grid.Column="1" SelectedDate="{Binding FromDate}" md:HintAssist.Hint="من تاريخ" Style="{StaticResource MaterialDesignOutlinedDatePicker}" Margin="8,0"/>
                <DatePicker Grid.Column="2" SelectedDate="{Binding ToDate}" md:HintAssist.Hint="إلى تاريخ" Style="{StaticResource MaterialDesignOutlinedDatePicker}" Margin="8,0"/>
                <Button Grid.Column="3" Command="{Binding RefreshCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Width="44" Height="44" Padding="0" Margin="8,0,0,0">
                    <md:PackIcon Kind="Refresh"/>
                </Button>
            </Grid>
        </md:Card>

        <md:Card Grid.Row="2" UniformCornerRadius="12" Background="White" Padding="0" md:ElevationAssist.Elevation="Dp2">
            <DataGrid ItemsSource="{Binding Entries}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                      Style="{StaticResource MaterialDesignDataGrid}" RowHeight="54" Background="Transparent">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="التاريخ والوقت" Binding="{Binding OccurredAt, StringFormat={}{0:yyyy/MM/dd HH:mm:ss}}" Width="165"/>
                    <DataGridTextColumn Header="المستخدم" Binding="{Binding UserName}" Width="150"/>
                    <DataGridTextColumn Header="العملية" Binding="{Binding Action}" Width="180"/>
                    <DataGridTextColumn Header="الكيان" Binding="{Binding EntityName}" Width="130"/>
                    <DataGridTextColumn Header="الرقم" Binding="{Binding EntityId}" Width="80"/>
                    <DataGridTextColumn Header="القيمة السابقة" Binding="{Binding OldValues}" Width="*"/>
                    <DataGridTextColumn Header="القيمة الجديدة / النتيجة" Binding="{Binding NewValues}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </md:Card>
    </Grid>
</UserControl>
